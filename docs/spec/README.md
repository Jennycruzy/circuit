# Specification

- `SPEC.md` — the main Circuit engineering specification (standing brief).
- `ADDENDUM-A.md` — governance veto module, benchmark corpus, revised order of work.

Both are plans, not authority: the live GenLayer docs win, and every divergence
is recorded in `PROGRESS.md` ("Doc divergences") with the evidence in
`docs/verification.md`.
